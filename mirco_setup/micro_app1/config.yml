pyms:
  services:
    requests:
      propagate_headers: true
    tracer:
      client: "jaeger"
      host: "localhost"
      component_name: "Python Microservice one"
  config:
    APP_NAME: "Python Microservice one"
    debug: true